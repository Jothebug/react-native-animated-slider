package com.animatedslider;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class AnimatedSliderSpec extends NativeAnimatedSliderSpec {
  AnimatedSliderSpec(ReactApplicationContext context) {
    super(context);
  }
}
